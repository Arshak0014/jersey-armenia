import React from 'react';

const NotFound = () => {
    return (
        <div style={{paddingTop: '150px'}} className="not_found_container">
            <div>
                not found :(
            </div>
        </div>
    );
};

export default NotFound;